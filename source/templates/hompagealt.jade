//In this webapp the homepage is the login page. Its the first view
//A user will see.

extend default

block content
	.login-page
		.form   
			form.login(action='/newpage', method = 'GET')
				p Inccorect username or password
				input(type='text', name='username', placeholder='username')
				|       
				input(type='password', name='password', placeholder='password')
				|       
				button(onclick="move(); type='submit', return false;") login